多态：同一个对象拥有多种形态
作用：把不同的数据类型进行统一

tips：
1. 把子类的对象赋值给父类的变量 -> 向上转型
   缺点：屏蔽了子类特有的方法
2. 把父类的变量转化回子类的变量 -> 向下转型
   向下转型有可能有风险，java要求必须要写强制类型转换

```
public class Animal{
	public void eat(){
		System.out.println("动物吃食物")：
	}
}
```