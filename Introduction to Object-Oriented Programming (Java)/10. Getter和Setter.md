成员变量一般使用private来声明，保护成员变量不被胡乱的赋值

setter主要是给成员变量赋值，做一定的保护
getter是从成员变量中获取数据

```
package k1;

public class Person {
//    String name;
//    int age;
    //成员变量私有
    private String name;
    private int age;
    
    public void setName(String name){
	    this.name = name;
    }
    
    //有效的保护成员变量
    public void setAge(int age){
	    if (age < 0){
		    this.age = 0;
	    } else {
		    this.age = age;
	    }
    }
    
    public void getName(S){
	    return this.name;
    }
    
    public void getAge(int age){
	    retuen this.age;
    }
    
    public void eat(){
        System.ou.println(this.name+"在吃东西");
    }
}
```

```
package k1;

public class TestPerson {
	public static void main(String[] args){
	    Person p = new Person();
//	    p.name = "Lana";
//	    p.age = -1; //语法没问题，逻辑有重大错误
	    p.setName("Lana");
	    p.setAge(-1);
	    
	    //查看这个人的名字
	    System.out.println(p.getName());
	    System.out.println(p.getAge());
	    
	    p.eat();
	}
}
```